import abi from "./abi/ThemisMetaNftBurning.json";
import { addresses, NETWORK_CHAINID } from "./index";

export const themisMetaNftBurningConfig = {
  address: addresses[NETWORK_CHAINID].themisMetaNftBurningAddress,
  abi,
};
